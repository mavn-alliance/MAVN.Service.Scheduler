apiVersion: v1
kind: Service
metadata:
  labels:
    app: scheduler
    environment: Development
  name: scheduler
  namespace: jobs
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: scheduler
  sessionAffinity: None
  type: ClusterIP
